CREATE DATABAE inventariohw;
USE inventariohw;

CREATE TABLE utente(
id INT PRIMARY KEY AUTO_INCREMENT,
nome VARCHAR(255),
cognome VARCHAR(255),
email VARCHAR(255),
password VARCHAR(64),
is_admin BOOLEAN);

CREATE TABLE aula(
id INT PRIMARY KEY AUTO_INCREMENT,
descrizione VARCHAR(255));

CREATE TABLE tipo(
id INT PRIMARY KEY AUTO_INCREMENT,
nome VARCHAR(255),
descrizione VARCHAR(255));

CREATE TABLE componente(
id INT PRIMARY KEY AUTO_INCREMENT,
marca VARCHAR(255),
numero_seriale VARCHAR(255),
data_installazione DATE,
utente_id INT,
tipo_id INT,
aula_id INT,
FOREIGN KEY (utente_id) REFERENCES utente(id),
FOREIGN KEY (tipo_id) REFERENCES tipo(id),
FOREIGN KEY (aula_id) REFERENCES aula(id));

INSERT INTO utente(nome, cognome, email, password, is_admin) 
VALUES('Andrea','Curti','andrea.curti@samtrevano.ch', 
'3190cb2e773d9aeda45d8386e2527717ab216b14022a306c9c3081bae65ad9d7', TRUE);